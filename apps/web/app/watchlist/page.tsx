import { PlaceholderPage } from "@/components/dashboard/placeholder-page";
import { productPages } from "@/lib/page-config";

export default function WatchlistPage() {
  return <PlaceholderPage config={productPages.watchlist} />;
}

